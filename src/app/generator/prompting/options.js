export const featuresMap = {
  TESTS: "Uses tests (unit and coverage)",
  NPM_DEPS: `Run "npm install" after generating`,
  GIT: "Uses git",
  RELEASE_NOTES: "Has a Release Notes file",
}

export const servicePackageList = {
  EXPRESS: "express",
  RESTIFY: "restify",
}
