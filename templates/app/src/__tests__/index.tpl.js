const {expect} = require("chai")
const {main} = require("../index")

describe("service main", function() {
  it("---- write your test here, config to be set-up ----", function() {
    expect(main()).to.eql(true)
  })
})
